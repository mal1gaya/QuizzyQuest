/**
 * Base url for requesting the application backend
 */
export const BASE_URL = "http://localhost:4000/api";
/**
 * Base url for requesting static images (files) on backend
 */
export const IMAGE_BASE_URL = "http://localhost:4000/";
/**
 * Minimum items of quiz
 */
export const MIN_ITEMS = 5;
/**
 * Maximum items of quiz
 */
export const MAX_ITEMS = 50;